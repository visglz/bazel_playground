# Playground for bazel stuff

- `cc_import_includes` - Check issues with `cc_import` and include directories
- `download_test` - Test downloads / connectivity compatible with Bazel 7 and 8 (without WORKSPACE file)

